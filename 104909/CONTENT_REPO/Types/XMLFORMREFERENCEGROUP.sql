CREATE OR REPLACE TYPE content_repo."XMLFORMREFERENCEGROUP"                                          as object (id number, rid number, name varchar2(250),start_date date, end_date date, nkid number, description varchar2(1000), entered_by number, modified number, deleted numbeR);
/